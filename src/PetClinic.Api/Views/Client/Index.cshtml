@model PetClinic.Api.ViewModels.ClientDashboardViewModel
@{
    Layout = "_ClientLayout";
    ViewData["Title"] = "Client Dashboard";

    var uiTexts = Model.UiTexts ?? new Dictionary<string, string>(StringComparer.OrdinalIgnoreCase);
    string T(string key, string fallback) => uiTexts.TryGetValue(key, out var value) ? value : fallback;
}

<div class="row g-3 mb-4">
    <div class="col-12">
        <h1 class="h3">@T("dashboard.welcome", "Welcome"), @Model.OwnerName</h1>
        <p class="text-muted mb-0">@T("dashboard.subtitle", "Manage your pets, appointments and invoices")</p>
    </div>
</div>

<div class="row g-3">
    <div class="col-md-4">
        <div class="card">
            <div class="card-body">
                <h5 class="card-title">@T("dashboard.pets", "My Pets")</h5>
                <p class="display-6">@Model.PetsCount</p>
            </div>
        </div>
    </div>
    <div class="col-md-4">
        <div class="card">
            <div class="card-body">
                <h5 class="card-title">@T("dashboard.appointments", "Appointments")</h5>
                <p class="display-6">@Model.AppointmentsCount</p>
            </div>
        </div>
    </div>
    <div class="col-md-4">
        <div class="card">
            <div class="card-body">
                <h5 class="card-title">@T("dashboard.invoices", "Invoices")</h5>
                <p class="display-6">@Model.InvoicesCount</p>
            </div>
        </div>
    </div>
</div>
